// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/thaiha1607/foursquare_server/ent/orderstatuscode"
	"github.com/thaiha1607/foursquare_server/ent/person"
	"github.com/thaiha1607/foursquare_server/ent/predicate"
	"github.com/thaiha1607/foursquare_server/ent/shipment"
	"github.com/thaiha1607/foursquare_server/ent/shipmenthistory"
)

// ShipmentHistoryQuery is the builder for querying ShipmentHistory entities.
type ShipmentHistoryQuery struct {
	config
	ctx           *QueryContext
	order         []shipmenthistory.OrderOption
	inters        []Interceptor
	predicates    []predicate.ShipmentHistory
	withShipment  *ShipmentQuery
	withPerson    *PersonQuery
	withOldStatus *OrderStatusCodeQuery
	withNewStatus *OrderStatusCodeQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ShipmentHistoryQuery builder.
func (shq *ShipmentHistoryQuery) Where(ps ...predicate.ShipmentHistory) *ShipmentHistoryQuery {
	shq.predicates = append(shq.predicates, ps...)
	return shq
}

// Limit the number of records to be returned by this query.
func (shq *ShipmentHistoryQuery) Limit(limit int) *ShipmentHistoryQuery {
	shq.ctx.Limit = &limit
	return shq
}

// Offset to start from.
func (shq *ShipmentHistoryQuery) Offset(offset int) *ShipmentHistoryQuery {
	shq.ctx.Offset = &offset
	return shq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (shq *ShipmentHistoryQuery) Unique(unique bool) *ShipmentHistoryQuery {
	shq.ctx.Unique = &unique
	return shq
}

// Order specifies how the records should be ordered.
func (shq *ShipmentHistoryQuery) Order(o ...shipmenthistory.OrderOption) *ShipmentHistoryQuery {
	shq.order = append(shq.order, o...)
	return shq
}

// QueryShipment chains the current query on the "shipment" edge.
func (shq *ShipmentHistoryQuery) QueryShipment() *ShipmentQuery {
	query := (&ShipmentClient{config: shq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := shq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := shq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shipmenthistory.Table, shipmenthistory.FieldID, selector),
			sqlgraph.To(shipment.Table, shipment.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, shipmenthistory.ShipmentTable, shipmenthistory.ShipmentColumn),
		)
		fromU = sqlgraph.SetNeighbors(shq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPerson chains the current query on the "person" edge.
func (shq *ShipmentHistoryQuery) QueryPerson() *PersonQuery {
	query := (&PersonClient{config: shq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := shq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := shq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shipmenthistory.Table, shipmenthistory.FieldID, selector),
			sqlgraph.To(person.Table, person.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, shipmenthistory.PersonTable, shipmenthistory.PersonColumn),
		)
		fromU = sqlgraph.SetNeighbors(shq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOldStatus chains the current query on the "old_status" edge.
func (shq *ShipmentHistoryQuery) QueryOldStatus() *OrderStatusCodeQuery {
	query := (&OrderStatusCodeClient{config: shq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := shq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := shq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shipmenthistory.Table, shipmenthistory.FieldID, selector),
			sqlgraph.To(orderstatuscode.Table, orderstatuscode.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, shipmenthistory.OldStatusTable, shipmenthistory.OldStatusColumn),
		)
		fromU = sqlgraph.SetNeighbors(shq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryNewStatus chains the current query on the "new_status" edge.
func (shq *ShipmentHistoryQuery) QueryNewStatus() *OrderStatusCodeQuery {
	query := (&OrderStatusCodeClient{config: shq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := shq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := shq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shipmenthistory.Table, shipmenthistory.FieldID, selector),
			sqlgraph.To(orderstatuscode.Table, orderstatuscode.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, shipmenthistory.NewStatusTable, shipmenthistory.NewStatusColumn),
		)
		fromU = sqlgraph.SetNeighbors(shq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ShipmentHistory entity from the query.
// Returns a *NotFoundError when no ShipmentHistory was found.
func (shq *ShipmentHistoryQuery) First(ctx context.Context) (*ShipmentHistory, error) {
	nodes, err := shq.Limit(1).All(setContextOp(ctx, shq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{shipmenthistory.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (shq *ShipmentHistoryQuery) FirstX(ctx context.Context) *ShipmentHistory {
	node, err := shq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ShipmentHistory ID from the query.
// Returns a *NotFoundError when no ShipmentHistory ID was found.
func (shq *ShipmentHistoryQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = shq.Limit(1).IDs(setContextOp(ctx, shq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{shipmenthistory.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (shq *ShipmentHistoryQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := shq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ShipmentHistory entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ShipmentHistory entity is found.
// Returns a *NotFoundError when no ShipmentHistory entities are found.
func (shq *ShipmentHistoryQuery) Only(ctx context.Context) (*ShipmentHistory, error) {
	nodes, err := shq.Limit(2).All(setContextOp(ctx, shq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{shipmenthistory.Label}
	default:
		return nil, &NotSingularError{shipmenthistory.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (shq *ShipmentHistoryQuery) OnlyX(ctx context.Context) *ShipmentHistory {
	node, err := shq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ShipmentHistory ID in the query.
// Returns a *NotSingularError when more than one ShipmentHistory ID is found.
// Returns a *NotFoundError when no entities are found.
func (shq *ShipmentHistoryQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = shq.Limit(2).IDs(setContextOp(ctx, shq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{shipmenthistory.Label}
	default:
		err = &NotSingularError{shipmenthistory.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (shq *ShipmentHistoryQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := shq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ShipmentHistories.
func (shq *ShipmentHistoryQuery) All(ctx context.Context) ([]*ShipmentHistory, error) {
	ctx = setContextOp(ctx, shq.ctx, "All")
	if err := shq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ShipmentHistory, *ShipmentHistoryQuery]()
	return withInterceptors[[]*ShipmentHistory](ctx, shq, qr, shq.inters)
}

// AllX is like All, but panics if an error occurs.
func (shq *ShipmentHistoryQuery) AllX(ctx context.Context) []*ShipmentHistory {
	nodes, err := shq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ShipmentHistory IDs.
func (shq *ShipmentHistoryQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if shq.ctx.Unique == nil && shq.path != nil {
		shq.Unique(true)
	}
	ctx = setContextOp(ctx, shq.ctx, "IDs")
	if err = shq.Select(shipmenthistory.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (shq *ShipmentHistoryQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := shq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (shq *ShipmentHistoryQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, shq.ctx, "Count")
	if err := shq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, shq, querierCount[*ShipmentHistoryQuery](), shq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (shq *ShipmentHistoryQuery) CountX(ctx context.Context) int {
	count, err := shq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (shq *ShipmentHistoryQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, shq.ctx, "Exist")
	switch _, err := shq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (shq *ShipmentHistoryQuery) ExistX(ctx context.Context) bool {
	exist, err := shq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ShipmentHistoryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (shq *ShipmentHistoryQuery) Clone() *ShipmentHistoryQuery {
	if shq == nil {
		return nil
	}
	return &ShipmentHistoryQuery{
		config:        shq.config,
		ctx:           shq.ctx.Clone(),
		order:         append([]shipmenthistory.OrderOption{}, shq.order...),
		inters:        append([]Interceptor{}, shq.inters...),
		predicates:    append([]predicate.ShipmentHistory{}, shq.predicates...),
		withShipment:  shq.withShipment.Clone(),
		withPerson:    shq.withPerson.Clone(),
		withOldStatus: shq.withOldStatus.Clone(),
		withNewStatus: shq.withNewStatus.Clone(),
		// clone intermediate query.
		sql:  shq.sql.Clone(),
		path: shq.path,
	}
}

// WithShipment tells the query-builder to eager-load the nodes that are connected to
// the "shipment" edge. The optional arguments are used to configure the query builder of the edge.
func (shq *ShipmentHistoryQuery) WithShipment(opts ...func(*ShipmentQuery)) *ShipmentHistoryQuery {
	query := (&ShipmentClient{config: shq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	shq.withShipment = query
	return shq
}

// WithPerson tells the query-builder to eager-load the nodes that are connected to
// the "person" edge. The optional arguments are used to configure the query builder of the edge.
func (shq *ShipmentHistoryQuery) WithPerson(opts ...func(*PersonQuery)) *ShipmentHistoryQuery {
	query := (&PersonClient{config: shq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	shq.withPerson = query
	return shq
}

// WithOldStatus tells the query-builder to eager-load the nodes that are connected to
// the "old_status" edge. The optional arguments are used to configure the query builder of the edge.
func (shq *ShipmentHistoryQuery) WithOldStatus(opts ...func(*OrderStatusCodeQuery)) *ShipmentHistoryQuery {
	query := (&OrderStatusCodeClient{config: shq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	shq.withOldStatus = query
	return shq
}

// WithNewStatus tells the query-builder to eager-load the nodes that are connected to
// the "new_status" edge. The optional arguments are used to configure the query builder of the edge.
func (shq *ShipmentHistoryQuery) WithNewStatus(opts ...func(*OrderStatusCodeQuery)) *ShipmentHistoryQuery {
	query := (&OrderStatusCodeClient{config: shq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	shq.withNewStatus = query
	return shq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ShipmentHistory.Query().
//		GroupBy(shipmenthistory.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (shq *ShipmentHistoryQuery) GroupBy(field string, fields ...string) *ShipmentHistoryGroupBy {
	shq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ShipmentHistoryGroupBy{build: shq}
	grbuild.flds = &shq.ctx.Fields
	grbuild.label = shipmenthistory.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.ShipmentHistory.Query().
//		Select(shipmenthistory.FieldCreatedAt).
//		Scan(ctx, &v)
func (shq *ShipmentHistoryQuery) Select(fields ...string) *ShipmentHistorySelect {
	shq.ctx.Fields = append(shq.ctx.Fields, fields...)
	sbuild := &ShipmentHistorySelect{ShipmentHistoryQuery: shq}
	sbuild.label = shipmenthistory.Label
	sbuild.flds, sbuild.scan = &shq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ShipmentHistorySelect configured with the given aggregations.
func (shq *ShipmentHistoryQuery) Aggregate(fns ...AggregateFunc) *ShipmentHistorySelect {
	return shq.Select().Aggregate(fns...)
}

func (shq *ShipmentHistoryQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range shq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, shq); err != nil {
				return err
			}
		}
	}
	for _, f := range shq.ctx.Fields {
		if !shipmenthistory.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if shq.path != nil {
		prev, err := shq.path(ctx)
		if err != nil {
			return err
		}
		shq.sql = prev
	}
	return nil
}

func (shq *ShipmentHistoryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ShipmentHistory, error) {
	var (
		nodes       = []*ShipmentHistory{}
		_spec       = shq.querySpec()
		loadedTypes = [4]bool{
			shq.withShipment != nil,
			shq.withPerson != nil,
			shq.withOldStatus != nil,
			shq.withNewStatus != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ShipmentHistory).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ShipmentHistory{config: shq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, shq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := shq.withShipment; query != nil {
		if err := shq.loadShipment(ctx, query, nodes, nil,
			func(n *ShipmentHistory, e *Shipment) { n.Edges.Shipment = e }); err != nil {
			return nil, err
		}
	}
	if query := shq.withPerson; query != nil {
		if err := shq.loadPerson(ctx, query, nodes, nil,
			func(n *ShipmentHistory, e *Person) { n.Edges.Person = e }); err != nil {
			return nil, err
		}
	}
	if query := shq.withOldStatus; query != nil {
		if err := shq.loadOldStatus(ctx, query, nodes, nil,
			func(n *ShipmentHistory, e *OrderStatusCode) { n.Edges.OldStatus = e }); err != nil {
			return nil, err
		}
	}
	if query := shq.withNewStatus; query != nil {
		if err := shq.loadNewStatus(ctx, query, nodes, nil,
			func(n *ShipmentHistory, e *OrderStatusCode) { n.Edges.NewStatus = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (shq *ShipmentHistoryQuery) loadShipment(ctx context.Context, query *ShipmentQuery, nodes []*ShipmentHistory, init func(*ShipmentHistory), assign func(*ShipmentHistory, *Shipment)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*ShipmentHistory)
	for i := range nodes {
		fk := nodes[i].ShipmentID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(shipment.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "shipment_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (shq *ShipmentHistoryQuery) loadPerson(ctx context.Context, query *PersonQuery, nodes []*ShipmentHistory, init func(*ShipmentHistory), assign func(*ShipmentHistory, *Person)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ShipmentHistory)
	for i := range nodes {
		fk := nodes[i].PersonID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(person.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "person_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (shq *ShipmentHistoryQuery) loadOldStatus(ctx context.Context, query *OrderStatusCodeQuery, nodes []*ShipmentHistory, init func(*ShipmentHistory), assign func(*ShipmentHistory, *OrderStatusCode)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ShipmentHistory)
	for i := range nodes {
		if nodes[i].OldStatusCode == nil {
			continue
		}
		fk := *nodes[i].OldStatusCode
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(orderstatuscode.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "old_status_code" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (shq *ShipmentHistoryQuery) loadNewStatus(ctx context.Context, query *OrderStatusCodeQuery, nodes []*ShipmentHistory, init func(*ShipmentHistory), assign func(*ShipmentHistory, *OrderStatusCode)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ShipmentHistory)
	for i := range nodes {
		if nodes[i].NewStatusCode == nil {
			continue
		}
		fk := *nodes[i].NewStatusCode
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(orderstatuscode.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "new_status_code" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (shq *ShipmentHistoryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := shq.querySpec()
	_spec.Node.Columns = shq.ctx.Fields
	if len(shq.ctx.Fields) > 0 {
		_spec.Unique = shq.ctx.Unique != nil && *shq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, shq.driver, _spec)
}

func (shq *ShipmentHistoryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(shipmenthistory.Table, shipmenthistory.Columns, sqlgraph.NewFieldSpec(shipmenthistory.FieldID, field.TypeUUID))
	_spec.From = shq.sql
	if unique := shq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if shq.path != nil {
		_spec.Unique = true
	}
	if fields := shq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, shipmenthistory.FieldID)
		for i := range fields {
			if fields[i] != shipmenthistory.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if shq.withShipment != nil {
			_spec.Node.AddColumnOnce(shipmenthistory.FieldShipmentID)
		}
		if shq.withPerson != nil {
			_spec.Node.AddColumnOnce(shipmenthistory.FieldPersonID)
		}
		if shq.withOldStatus != nil {
			_spec.Node.AddColumnOnce(shipmenthistory.FieldOldStatusCode)
		}
		if shq.withNewStatus != nil {
			_spec.Node.AddColumnOnce(shipmenthistory.FieldNewStatusCode)
		}
	}
	if ps := shq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := shq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := shq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := shq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (shq *ShipmentHistoryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(shq.driver.Dialect())
	t1 := builder.Table(shipmenthistory.Table)
	columns := shq.ctx.Fields
	if len(columns) == 0 {
		columns = shipmenthistory.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if shq.sql != nil {
		selector = shq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if shq.ctx.Unique != nil && *shq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range shq.predicates {
		p(selector)
	}
	for _, p := range shq.order {
		p(selector)
	}
	if offset := shq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := shq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ShipmentHistoryGroupBy is the group-by builder for ShipmentHistory entities.
type ShipmentHistoryGroupBy struct {
	selector
	build *ShipmentHistoryQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (shgb *ShipmentHistoryGroupBy) Aggregate(fns ...AggregateFunc) *ShipmentHistoryGroupBy {
	shgb.fns = append(shgb.fns, fns...)
	return shgb
}

// Scan applies the selector query and scans the result into the given value.
func (shgb *ShipmentHistoryGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, shgb.build.ctx, "GroupBy")
	if err := shgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ShipmentHistoryQuery, *ShipmentHistoryGroupBy](ctx, shgb.build, shgb, shgb.build.inters, v)
}

func (shgb *ShipmentHistoryGroupBy) sqlScan(ctx context.Context, root *ShipmentHistoryQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(shgb.fns))
	for _, fn := range shgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*shgb.flds)+len(shgb.fns))
		for _, f := range *shgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*shgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := shgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ShipmentHistorySelect is the builder for selecting fields of ShipmentHistory entities.
type ShipmentHistorySelect struct {
	*ShipmentHistoryQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (shs *ShipmentHistorySelect) Aggregate(fns ...AggregateFunc) *ShipmentHistorySelect {
	shs.fns = append(shs.fns, fns...)
	return shs
}

// Scan applies the selector query and scans the result into the given value.
func (shs *ShipmentHistorySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, shs.ctx, "Select")
	if err := shs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ShipmentHistoryQuery, *ShipmentHistorySelect](ctx, shs.ShipmentHistoryQuery, shs, shs.inters, v)
}

func (shs *ShipmentHistorySelect) sqlScan(ctx context.Context, root *ShipmentHistoryQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(shs.fns))
	for _, fn := range shs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*shs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := shs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
