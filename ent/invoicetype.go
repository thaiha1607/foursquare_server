// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/thaiha1607/foursquare_server/ent/invoicetype"
)

// InvoiceType is the model entity for the InvoiceType schema.
type InvoiceType struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// InvoiceType holds the value of the "invoice_type" field.
	InvoiceType  string `json:"invoice_type,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*InvoiceType) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case invoicetype.FieldID:
			values[i] = new(sql.NullInt64)
		case invoicetype.FieldInvoiceType:
			values[i] = new(sql.NullString)
		case invoicetype.FieldCreatedAt, invoicetype.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the InvoiceType fields.
func (it *InvoiceType) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case invoicetype.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			it.ID = int(value.Int64)
		case invoicetype.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				it.CreatedAt = value.Time
			}
		case invoicetype.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				it.UpdatedAt = value.Time
			}
		case invoicetype.FieldInvoiceType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field invoice_type", values[i])
			} else if value.Valid {
				it.InvoiceType = value.String
			}
		default:
			it.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the InvoiceType.
// This includes values selected through modifiers, order, etc.
func (it *InvoiceType) Value(name string) (ent.Value, error) {
	return it.selectValues.Get(name)
}

// Update returns a builder for updating this InvoiceType.
// Note that you need to call InvoiceType.Unwrap() before calling this method if this InvoiceType
// was returned from a transaction, and the transaction was committed or rolled back.
func (it *InvoiceType) Update() *InvoiceTypeUpdateOne {
	return NewInvoiceTypeClient(it.config).UpdateOne(it)
}

// Unwrap unwraps the InvoiceType entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (it *InvoiceType) Unwrap() *InvoiceType {
	_tx, ok := it.config.driver.(*txDriver)
	if !ok {
		panic("ent: InvoiceType is not a transactional entity")
	}
	it.config.driver = _tx.drv
	return it
}

// String implements the fmt.Stringer.
func (it *InvoiceType) String() string {
	var builder strings.Builder
	builder.WriteString("InvoiceType(")
	builder.WriteString(fmt.Sprintf("id=%v, ", it.ID))
	builder.WriteString("created_at=")
	builder.WriteString(it.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(it.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("invoice_type=")
	builder.WriteString(it.InvoiceType)
	builder.WriteByte(')')
	return builder.String()
}

// InvoiceTypes is a parsable slice of InvoiceType.
type InvoiceTypes []*InvoiceType
